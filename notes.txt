ECR = repository pentru imagini de docker in AWS

Amazon ECS (Elastic Container Service) este un serviciu AWS care te ajută să rulezi aplicații containerizate (Docker) în cloud.

Cluster = grup de servere pt containere